const Config = {
  screenWidth: 500,
  screenHeight: 500,
  boardWidth: 10,
  boardHeight: 15,
  boardScale: 1.5,
  boardXOffset: 120,
  boardYOffset: 60,
  tileSize: 16,
  initialGameFrequency: 1,
  initialDropFrequency: 0.02
};

export default Object.freeze(Config);
